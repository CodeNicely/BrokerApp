<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ujjwal.broker">
<uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".helper.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".SplashScreen.View.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".WelcomeScreen.WelcomeSliderScreen" />
        <activity android:name=".Login.View.LoginMainImp"
                  android:theme="@style/AppTheme"></activity>
<activity android:name=".OtpVerification.View.OtpActivity"/>
        <activity
            android:name=".Deals.View.DealsActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".Buy.View.BuyFragment"/>
        </activity>
    </application>

</manifest>